You are fixing 3 bugs in this Next.js portraitpay app. Follow karpathy-skills principles: Think Before Coding, Simplicity First, Surgical Changes, Goal-Driven Execution.

Task 1 - /api/portraits 500 error:
Check src/app/api/portrait/route.ts - this API returns 500 on production. The Portrait model in Prisma has unmigrated fields: allowAiLicensing, aiLicenseFee, aiLicenseScopes, aiProhibitedScopes, aiTerritorialScope. The API might be selecting fields that do not exist in the DB. Fix the root cause. Run local build to verify.

Task 2 - /portraits/upload failure:
The upload flow fails. Check what env vars the upload route needs (ETH_WALLET_PRIVATE_KEY, PINATA_JWT, AWS_*) vs what is configured. Read the upload API route and identify which specific env vars are actually used and which are missing/empty. Report findings.

Task 3 - /kyc failure:
The KYC form submission fails because the frontend does not send required fields: idCardNumberHash and faceMatchScore. Backend at src/app/api/kyc/route.ts expects these. Find the KYC form component, then add:
- idCardNumberHash: SHA-256 hash of the ID card number (use crypto.subtle.digest in browser)
- faceMatchScore: similarity score from face-api.js face comparison

Commit each fix separately with a clear commit message after you verify it works. Push to origin main after each commit.
